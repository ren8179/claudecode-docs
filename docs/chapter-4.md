# 第四章：实用技巧与工作流（详细版）

---

## 4.1 效率提升技巧

### 4.1.1 基础操作技巧

#### 项目初始化与配置

**快速初始化新项目：**

```bash
# 一套完整的初始化流程
mkdir my-project
cd my-project
git init
claude /init

# Claude会自动：
# 1. 扫描项目结构
# 2. 分析技术栈
# 3. 生成CLAUDE.md
# 4. 创建配置文件
```

**快速引用上下文：**

```bash
# 引用单个文件
@src/auth/userAuth.ts

# 引用整个目录
@src/components/

# 模糊匹配
@auth  # 匹配所有包含auth的文件

# 组合引用
@src/auth.ts @src/components/Button.tsx
```

**即时执行Bash命令：**

```bash
# 在Claude Code中直接运行命令
!git status
!npm test
!git log --oneline -10

# 组合使用
请查看@src/auth.ts的代码，然后运行!npm test查看测试结果
```

#### 会话管理技巧

**无缝续接会话：**

```bash
# 恢复上一次对话
claude --continue

# 查看历史会话列表
claude --resume

# 按名称恢复会话
claude --resume feature-auth

# 查看当前会话信息
/session
```

**会话命名与管理：**

```bash
# 给当前会话命名
/rename feature-auth

# 查看所有会话
/sessions

# 删除会话
/session delete [session-id]
```

**上下文可视化：**

```bash
# 查看token使用情况
/context

# 输出示例：
Context Usage: 45,234 / 200,000 tokens (22.6%)
├── System Prompt: 1,234 tokens
├── CLAUDE.md: 3,456 tokens
├── Conversation: 38,543 tokens
└── Files: 2,001 tokens

Performance: ⚠️ Moderate (建议压缩上下文)
```

#### 高效快捷键

| 快捷键 | 功能 | 使用场景 |
|--------|------|---------|
| `Shift+Tab` | 进入对话模式 | 讨论架构、询问问题 |
| `Shift+Tab×2` | 进入Plan模式 | 复杂任务规划 |
| `Ctrl+R` | 反向搜索历史提示词 | 快速找到之前的命令 |
| `Ctrl+S` | 暂存当前提示词 | 临时保存想法 |
| `ESC ESC` | 回退操作 | 撤销上一步 |
| `Ctrl+L` | 清屏 | 清理终端显示 |
| `Ctrl+C` | 退出Claude Code | 结束会话 |

**Plan模式快捷键详解：**

```
正常编辑模式：
按一次 Shift+Tab → 对话模式（Chat Mode）
  ↓
  Claude只回答问题，不执行任何操作
  适合：讨论架构、询问建议

按两次 Shift+Tab → Plan模式（Plan Mode）
  ↓
  Claude先规划后执行
  适合：复杂任务、架构重构

按三次 Shift+Tab → 回到编辑模式
  ↓
  正常的编码模式
```

### 4.1.2 效率提升工作流

#### 工作流1：测试驱动开发（TDD）

**为什么TDD很重要？**

```
传统开发流程：
需求 → 写代码 → 写测试 → 发现bug → 修改代码 → 循环

TDD流程：
需求 → 写测试 → 运行测试(失败) → 写代码 → 运行测试(通过) → 重构

TDD的优势：
✅ 测试覆盖率高
✅ 代码质量好
✅ 重构有保障
✅ 文档自动生成
```

**Claude Code的TDD工作流：**

```bash
# 第1步：先写测试（不写实现代码）
请为用户注册功能编写测试用例，包括：
1. 成功注册
2. 邮箱已存在
3. 密码不符合要求
4. 网络错误

使用Jest框架

# 第2步：运行测试，确认全部失败
运行测试，确认所有测试用例都是红色的（失败）

# 第3步：实现功能，让测试通过
现在实现用户注册功能，让刚才的测试全部通过
不要修改测试代码

# 第4步：重构代码
现在优化代码结构，但保持测试通过

# 第5步：验证最终结果
运行测试，确认全部通过，并查看覆盖率
```

#### 工作流2：增量式开发

**为什么增量开发很重要？**

```
❌ 一次性完成大任务的问题：
- Token消耗多
- Claude容易"遗忘"要求
- 出问题难以定位
- 难以回滚

✅ 增量开发的优势：
- 每个任务小而专注
- 完成后立即清理上下文
- 出问题容易定位和修复
- 可以灵活调整计划
```

**增量开发实战案例：**

```bash
# === 第一个增量：基础数据模型 ===
请帮我创建用户数据模型，包括：
- id: string (UUID)
- email: string (unique)
- password: string (hashed)
- createdAt: Date
- updatedAt: Date

使用TypeScript + Prisma

# 完成后清理上下文
/clear

# === 第二个增量：注册API ===
请创建用户注册API端点：
- POST /api/auth/register
- 验证邮箱格式
- 验证密码强度
- 返回JWT token

使用Express + TypeScript

# 完成后清理上下文
/clear

# === 第三个增量：登录API ===
请创建用户登录API端点：
- POST /api/auth/login
- 验证邮箱和密码
- 返回JWT token

# 完成后清理上下文
/clear

# === 第四个增量：认证中间件 ===
请创建JWT认证中间件：
- 验证token有效性
- 解析用户信息
- 注入到req对象

# 完成后清理上下文
/clear

# === 第五个增量：测试 ===
请为以上所有功能编写完整的测试用例
```

#### 工作流3：代码审查工作流

**个人代码审查：**

```bash
# 第1步：让Claude自我审查
请详细审查刚才写的代码，检查：
1. 是否符合CLAUDE.md中的规范
2. 是否有潜在的bug
3. 是否有安全问题
4. 性能是否有优化空间
5. 代码是否清晰易懂

# 第2步：运行测试
运行测试，确保所有测试通过

# 第3步：检查代码覆盖率
检查测试覆盖率，确保达到80%以上

# 第4步：生成文档
为这段代码生成文档，包括：
1. 功能描述
2. 使用示例
3. API文档

# 第5步：提交代码
git add .
git commit -m "feat: 添加用户认证功能"
```

**团队代码审查工作流：**

```bash
# 第1步：创建feature分支
git checkout -b feature-user-auth

# 第2步：使用Plan模式开发
Shift+Tab × 2
[开发用户认证功能]

# 第3步：自我审查
请按照CLAUDE.md中的规范，严格审查当前代码
特别关注：
- 代码风格
- 错误处理
- 安全问题

# 第4步：运行完整测试
npm run test:all

# 第5步：创建PR
请使用github MCP创建PR：
- 标题：feat: 添加用户认证功能
- 描述：[自动生成]
- 标签：feature, authentication
```

---

## 4.2 常用斜杠命令速查

### 4.2.1 系统内置命令

#### 核心命令（高频使用）

| 命令 | 功能 | 使用频率 | 示例 |
|------|------|---------|------|
| `/clear` | 清空对话历史 | ⭐⭐⭐⭐⭐ | 开始新任务前使用 |
| `/compact` | 压缩对话但保留摘要 | ⭐⭐⭐⭐⭐ | 上下文过长时使用 |
| `/context` | 可视化上下文使用 | ⭐⭐⭐⭐⭐ | 检查token使用情况 |
| `/model` | 切换模型 | ⭐⭐⭐⭐ | 切换快速/思考模型 |
| `/init` | 项目初始化 | ⭐⭐⭐⭐ | 新项目开始时使用 |
| `/plan` | 进入Plan模式 | ⭐⭐⭐⭐ | 复杂任务规划 |
| `/help` | 显示帮助信息 | ⭐⭐⭐ | 忘记命令时使用 |

**详细使用说明：**

```bash
# /clear - 清空对话
/clear
# 适用于：开始全新任务、上下文混乱时

# /compact - 压缩对话
/compact
# 适用于：想保留上下文摘要但释放token

# /context - 查看上下文
/context
# 输出示例：
# Context: 45,234 / 200,000 tokens (22.6%)
# Performance: ✅ Good

# /model - 切换模型
/model          # 查看当前模型
/model sonnet   # 切换到Sonnet（快速）
/model opus     # 切换到Opus（深度思考）

# /init - 初始化项目
/init
# 自动扫描项目并生成CLAUDE.md

# /plan - 进入Plan模式
/plan
# 开始规划任务
```

#### 会话管理命令

| 命令 | 功能 | 说明 |
|------|------|------|
| `/rename` | 重命名当前会话 | 方便后续查找 |
| `/sessions` | 查看所有会话 | 列出历史会话 |
| `/session` | 会话信息管理 | 查看当前会话详情 |
| `/resume` | 恢复会话 | 继续之前的对话 |

```bash
# /rename - 重命名会话
/rename feature-auth
# 将当前会话命名为"feature-auth"

# /sessions - 查看所有会话
/sessions
# 输出示例：
# 1. feature-auth (2 hours ago)
# 2. bug-fix-login (5 hours ago)
# 3. refactor-api (1 day ago)

# /session - 会话信息
/session
# 输出当前会话的详细信息

# /resume - 恢复会话
/resume feature-auth
# 恢复名为"feature-auth"的会话
```

#### 配置管理命令

| 命令 | 功能 | 说明 |
|------|------|------|
| `/config` | 查看修改配置 | 管理Claude Code配置 |
| `/memory` | 编辑项目记忆 | 修改CLAUDE.md |
| `/permissions` | 管理工具权限 | 设置工具使用权限 |
| `/hooks` | 配置钩子 | 设置自动化脚本 |
| `/mcp` | 管理MCP服务器 | 添加/删除MCP服务 |

```bash
# /config - 查看配置
/config
# 查看当前配置信息

# /memory - 编辑CLAUDE.md
/memory
# 打开编辑器修改CLAUDE.md

# /permissions - 管理权限
/permissions
# 设置哪些工具需要确认

# /hooks - 配置钩子
/hooks
# 配置自动化脚本

# /mcp - 管理MCP
/mcp
# 进入MCP配置界面
```

### 4.2.2 自定义斜杠命令

#### 创建自定义命令

**命令目录结构：**

```
项目目录/
├── .claude/
│   └── commands/        # 项目级命令
│       ├── review.md
│       ├── test.md
│       └── deploy.md
└── 用户目录/
    └── .claude/
        └── commands/    # 用户级命令（全局）
            ├── fix-issue.md
            └── optimize.md
```

#### 常用自定义命令示例

**命令1：代码审查**

```markdown
# .claude/commands/review.md

# 代码审查

对 $ARGUMENTS 指定的代码进行严格审查：

## 审查项目

### 1. 代码风格
- 是否符合CLAUDE.md中的规范
- 命名是否清晰
- 缩进和格式是否一致

### 2. 功能正确性
- 逻辑是否正确
- 是否有边界情况处理
- 是否有潜在bug

### 3. 安全性
- 是否有SQL注入风险
- 是否有XSS风险
- 是否正确处理用户输入
- 敏感信息是否安全

### 4. 性能
- 是否有不必要的循环
- 是否有内存泄漏风险
- 数据库查询是否优化
- 缓存策略是否合理

### 5. 可维护性
- 代码是否清晰易懂
- 是否有足够的注释
- 是否遵循DRY原则
- 函数是否职责单一

## 输出格式

请按以下格式输出审查结果：

### ✅ 优点
- [列出代码的优点]

### ⚠️ 问题
- [列出发现的问题，按严重程度排序]

### 💡 建议
- [给出改进建议]

### 📋 TODO
- [创建待办事项列表]
```

**使用方法：**

```bash
/review src/auth/userAuth.ts
/review src/components/
```

**命令2：自动化测试**

```markdown
# .claude/commands/test.md

# 运行测试

为 $ARGUMENTS 指定的模块运行完整测试流程：

## 测试流程

### 1. 单元测试
运行单元测试并检查覆盖率

### 2. 集成测试
运行集成测试

### 3. E2E测试
运行端到端测试

### 4. 性能测试
运行性能基准测试

### 5. 生成报告
生成测试报告，包括：
- 测试通过率
- 覆盖率
- 性能指标
- 失败用例详情

## 如果测试失败

1. 分析失败原因
2. 提供修复建议
3. 创建GitHub Issue（如果需要）
```

**使用方法：**

```bash
/test auth
/test components/Button
```

**命令3：自动部署**

```markdown
# .claude/commands/deploy.md

# 自动部署

执行 $ARGUMENTS 环境的部署流程：

## 部署前检查

1. 检查当前分支
2. 确保没有未提交的更改
3. 运行测试套件
4. 运行代码检查
5. 构建生产版本

## 部署流程

1. 备份当前版本
2. 部署新版本
3. 运行健康检查
4. 验证核心功能
5. 监控错误日志

## 部署后

1. 生成部署报告
2. 通知团队
3. 更新文档

## 回滚方案

如果部署失败，自动回滚到上一版本
```

**使用方法：**

```bash
/deploy production
/deploy staging
```

**命令4：GitHub Issue管理**

```markdown
# .claude/commands/fix-issue.md

# 修复GitHub Issue

自动修复GitHub Issue $ARGUMENTS：

## 工作流程

1. 从GitHub获取Issue详情
2. 分析Issue描述
3. 使用Plan模式制定修复计划
4. 实现修复
5. 编写测试
6. 提交代码
7. 创建PR
8. 关联Issue

## 输出

完成后生成报告，包括：
- 修复摘要
- 代码变更
- 测试结果
- PR链接
```

**使用方法：**

```bash
/fix-issue 123
```

### 4.2.3 命令组合使用

#### 常用命令组合

**组合1：开始新功能开发**

```bash
# 清空上下文
/clear

# 进入Plan模式
/plan

# 开始规划
请实现用户认证功能
```

**组合2：代码审查与修复**

```bash
# 审查代码
/review src/auth/

# 如果发现问题，让Claude修复
请根据刚才的审查结果修复这些问题

# 运行测试
/test auth

# 提交代码
!git add . && git commit -m "fix: 修复认证问题"
```

**组合3：部署流程**

```bash
# 检查当前状态
!git status

# 运行测试
npm test

# 部署
/deploy production
```

---

## 4.3 上下文管理技巧

### 4.3.1 理解上下文窗口

#### 上下文窗口的局限性

**重要认知：**

```
┌─────────────────────────────────────────────────────────┐
│                  上下文性能曲线                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 性能 100% ████                                          │
│          │                                              │
│ 性能  80% ████  ← 开始下降                              │
│          │   █                                          │
│ 性能  60% ████  █                                       │
│          │   ████                                       │
│ 性能  40% ████  █████                                   │
│          │   ██████                                    │
│ 性能  20% ████  ████████                                │
│          │   ██████████                                │
│          0%───25%───50%───75%──100%                     │
│               上下文使用率                              │
│                                                         │
│ 关键发现：                                              │
│ - 20-40%使用率时性能就开始下降                          │
│ - 不要等到100%才压缩                                    │
│ - 建议保持在50%以下                                     │
└─────────────────────────────────────────────────────────┘
```

**上下文包含的内容：**

```bash
# 查看上下文详细构成
/context

# 输出示例：
Context Breakdown:
├── System Prompt:     1,234 tokens  (2.7%)
├── CLAUDE.md:         3,456 tokens  (7.6%)
├── Conversation:     38,543 tokens  (85.0%)
├── Files Referenced:  2,001 tokens  (4.4%)
└── Skills Loaded:        123 tokens  (0.3%)
└────────────────────────────────────
Total:                45,357 / 200,000 (22.7%)

Performance: ✅ Good
Recommendation: 继续使用，无需压缩
```

### 4.3.2 上下文管理策略

#### 策略1：复制-粘贴重置法

**适用于：** 上下文臃肿，Claude开始"遗忘"要求时

```bash
# 第1步：复制重要内容
# 手动复制当前对话中的关键信息：
# - 项目背景
# - 技术栈
# - 当前任务
# - 重要的决策

# 第2步：运行/compact获取摘要
/compact

# Claude会生成对话摘要：
# Conversation Summary:
# 1. 讨论了用户认证功能的设计
# 2. 决定使用JWT进行token管理
# 3. 实现了注册和登录API
# 4. 正在实现认证中间件

# 第3步：运行/clear清空上下文
/clear

# 第4步：粘贴关键信息回去
# 项目：电商平台用户认证功能
# 技术栈：Express + TypeScript + PostgreSQL
# 当前任务：实现JWT认证中间件
# 关键决策：使用JWT，token有效期24小时

请继续实现JWT认证中间件
```

#### 策略2：限定对话范围

**原则：** 每个功能或任务使用一个独立对话

```
❌ 错误做法：一个对话完成所有事情
同一个对话中：
- 构建认证系统
- 重构数据库层
- 更新前端UI
- 编写测试
- 部署到生产

结果：
- 上下文过长
- Claude容易"遗忘"早期要求
- 出问题难以定位

✅ 正确做法：每个功能独立对话
对话1：认证系统开发
对话2：数据库层重构
对话3：前端UI更新
对话4：测试编写
对话5：部署流程
```

**实战示例：**

```bash
# === 对话1：认证系统开发 ===
claude --session auth-system
请实现用户认证功能
[...开发过程...]
/clear

# === 对话2：数据库重构 ===
claude --session database-refactor
请重构用户表结构
[...重构过程...]
/clear

# === 对话3：前端UI ===
claude --session frontend-ui
请创建登录页面
[...UI开发过程...]
/clear

# === 对话4：测试 ===
claude --session testing
请为认证功能编写测试
[...测试过程...]
/clear

# === 对话5：部署 ===
claude --session deployment
请部署到生产环境
[...部署过程...]
```

#### 策略3：增量式上下文管理

**适用于：** 长期项目，持续开发

```bash
# 第1天：项目初始化
claude --session day-1-init
/init
[生成CLAUDE.md]

# 第2天：功能开发
claude --session day-2-auth
# Claude自动读取CLAUDE.md
请实现用户认证功能
[...开发...]

# 第3天：继续开发
claude --session day-3-payment
# Claude自动读取CLAUDE.md
请实现支付功能
[...开发...]

# 优势：
# - 每天的上下文独立
# - CLAUDE.md保持项目知识
# - 容易回溯和定位问题
```

### 4.3.3 上下文优化技巧

#### 技巧1：精确引用文件

**❌ 低效做法：**

```bash
# 让Claude自己搜索
请修改用户认证相关的代码

# Claude需要：
# 1. 搜索所有相关文件
# 2. 读取每个文件
# 3. 判断哪些需要修改
# 浪费大量token
```

**✅ 高效做法：**

```bash
# 精确指定文件
请修改@src/auth/userAuth.ts中的登录逻辑，
参考@src/auth/types.ts中的类型定义

# Claude：
# 1. 只读取指定的文件
# 2. 直接进行修改
# 节省大量token
```

#### 技巧2：使用CLAUDE.md减少重复说明

**❌ 低效做法：**

```bash
# 每次都要重复规范
请创建一个用户组件，使用PascalCase命名，
文件名用kebab-case，函数用camelCase...
[大量规范描述...]
```

**✅ 高效做法：**

```bash
# 在CLAUDE.md中定义规范
# CLAUDE.md
## 命名规范
- 组件名：PascalCase
- 文件名：kebab-case
- 函数：camelCase

# 简洁的指令
请创建一个用户组件，确保代码严格遵守CLAUDE.md中的规范
```

#### 技巧3：善用/compact而非/clear

**何时使用/compact：**

```bash
# 想保留上下文摘要时
/compact

# Claude生成摘要：
## 项目进度摘要
1. 完成了用户注册功能
2. 实现了JWT token生成
3. 创建了认证中间件
4. 待完成：密码重置功能

## 技术决策
- 使用bcrypt加密密码
- JWT有效期24小时
- 使用Redis存储黑名单token

## 下一步
实现密码重置功能

# 继续开发
请实现密码重置功能
```

**何时使用/clear：**

```bash
# 上下文完全混乱时
/clear

# 开始全新任务
请实现订单管理功能
```

#### 技巧4：使用子代理隔离上下文

**场景：** 需要同时处理多个独立任务

```bash
# ❌ 错误做法：在一个对话中处理
请同时：
1. 审查认证代码
2. 编写测试用例
3. 生成API文档
4. 优化性能

# 问题：
# - 上下文混乱
# - 容易"遗忘"要求
# - 难以追踪进度

# ✅ 正确做法：使用子代理
请帮我并行执行以下任务：
1. 使用code-reviewer agent审查@src/auth/
2. 使用test-writer agent为认证功能编写测试
3. 使用doc-generator agent生成API文档
4. 使用performance-tuner agent优化性能

# Claude创建4个子代理，每个有独立上下文
```

---

## 4.4 高级工作流

### 4.4.1 完整的开发工作流

#### 从需求到部署的完整流程

```
┌─────────────────────────────────────────────────────────┐
│              Claude Code 完整开发工作流                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 需求阶段                                            │
│     - 讨论需求                                          │
│     - 明确边界                                          │
│     - 确定技术方案                                      │
│         ↓                                               │
│  2. 规划阶段                                            │
│     - 进入Plan模式                                      │
│     - 生成详细计划                                      │
│     - 确认实施方案                                      │
│         ↓                                               │
│  3. 开发阶段                                            │
│     - 增量式开发                                        │
│     - TDD工作流                                        │
│     - 定期提交                                          │
│         ↓                                               │
│  4. 测试阶段                                            │
│     - 单元测试                                          │
│     - 集成测试                                          │
│     - E2E测试                                           │
│         ↓                                               │
│  5. 审查阶段                                            │
│     - 自我审查                                          │
│     - 同行审查                                          │
│     - 修复问题                                          │
│         ↓                                               │
│  6. 部署阶段                                            │
│     - 构建生产版本                                      │
│     - 执行部署                                          │
│     - 验证部署                                          │
│         ↓                                               │
│  7. 监控阶段                                            │
│     - 监控错误                                          │
│     - 收集反馈                                          │
│     - 迭代优化                                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**实战案例：电商购物车功能**

```bash
# === 阶段1：需求讨论 ===
请帮我分析购物车功能的需求，包括：
1. 添加商品到购物车
2. 修改商品数量
3. 删除商品
4. 计算总价
5. 持久化存储

# === 阶段2：规划 ===
Shift+Tab × 2
请为购物车功能制定详细实施计划

# === 阶段3：开发（增量式） ===
# 3.1 数据模型
请创建购物车数据模型
/clear

# 3.2 API端点
请创建购物车API端点
/clear

# 3.3 前端组件
请创建购物车前端组件
/clear

# === 阶段4：测试 ===
请为购物车功能编写完整测试
/clear

# === 阶段5：审查 ===
/reverse cart
请审查购物车代码
/clear

# === 阶段6：部署 ===
/deploy staging
/clear
```

### 4.4.2 团队协作工作流

#### 团队协作的最佳实践

**共享CLAUDE.md：**

```bash
# 1. 将CLAUDE.md纳入版本控制
git add CLAUDE.md
git commit -m "docs: 添加团队代码规范"
git push origin main

# 2. 团队成员克隆项目后
git pull origin main
# Claude Code自动读取CLAUDE.md

# 3. 团队成员更新规范
# 在PR中发现问题
@claude 请把这次教训写入CLAUDE.md：
不要使用any类型，要明确定义类型

# 4. 提交更新
git add CLAUDE.md
git commit -m "docs: 更新类型规范"
git push origin main

# 5. 整个团队受益
# 所有Claude Code实例都会自动应用新规范
```

**共享Skills：**

```bash
# 1. 在项目目录创建skills
mkdir -p .claude/skills

# 2. 将团队Skills放入该目录
.claude/skills/
├── company-brand-guidelines/
├── company-design-system/
└── company-api-standards/

# 3. 纳入版本控制
git add .claude/skills/
git commit -m "docs: 添加团队Skills"
git push origin main

# 4. 团队成员自动获得这些Skills
git pull
# 重启Claude Code，Skills自动可用
```

**共享Slash Commands：**

```bash
# 1. 创建团队命令
mkdir -p .claude/commands

# 2. 创建常用命令
.claude/commands/
├── review.md      # 代码审查
├── test.md        # 运行测试
├── deploy.md      # 部署
└── format.md      # 代码格式化

# 3. 纳入版本控制
git add .claude/commands/
git commit -m "docs: 添加团队命令"
git push origin main
```

### 4.4.3 故障排查工作流

#### 系统化的问题解决流程

```
┌─────────────────────────────────────────────────────────┐
│              Claude Code 故障排查工作流                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 问题识别                                            │
│     - 明确问题现象                                      │
│     - 收集错误信息                                      │
│     - 确定影响范围                                      │
│         ↓                                               │
│  2. 信息收集                                            │
│     - 查看日志                                          │
│     - 检查代码                                          │
│     - 运行测试                                          │
│         ↓                                               │
│  3. 原因分析                                            │
│     - 使用Claude分析                                    │
│     - 识别根本原因                                      │
│     - 确定修复方案                                      │
│         ↓                                               │
│  4. 实施修复                                            │
│     - 编写修复代码                                      │
│     - 添加测试用例                                      │
│     - 验证修复效果                                      │
│         ↓                                               │
│  5. 预防措施                                            │
│     - 更新CLAUDE.md                                     │
│     - 添加回归测试                                      │
│     - 分享经验教训                                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**实战案例：修复API性能问题**

```bash
# === 1. 问题识别 ===
用户报告API响应很慢，超过5秒

# === 2. 信息收集 ===
请查看以下信息：
1. error.log中的错误
2. API端点代码：@src/api/orders.ts
3. 数据库查询：@src/db/queries.ts

# === 3. 原因分析 ===
请分析为什么订单API响应慢，
找出性能瓶颈并给出优化建议

# Claude可能发现：
# - N+1查询问题
# - 缺少索引
# - 不必要的数据加载

# === 4. 实施修复 ===
请根据分析结果优化性能

# === 5. 预防措施 ===
@claude 请把这次教训写入CLAUDE.md：
- 永远避免N+1查询
- 为外键添加索引
- 使用select指定需要字段

# 添加回归测试
请为性能优化添加测试用例
```

---

## 本章重点回顾

### 效率提升核心技巧

| 技巧 | 关键点 |
|------|--------|
| **精确引用** | 使用@指定文件，避免Claude搜索 |
| **CLAUDE.md** | 减少重复说明，保持规范一致 |
| **增量开发** | 每个任务小而专注，完成后/clear |
| **Plan模式** | 复杂任务先规划后执行 |
| **子代理** | 并行处理独立任务 |
| **自定义命令** | 封装常用工作流 |

### 上下文管理核心原则

| 原则 | 说明 |
|------|------|
| **及时压缩** | 20-40%使用率时就要考虑压缩 |
| **限定范围** | 一个对话一个任务 |
| **精确引用** | 用@指定文件而非让Claude搜索 |
| **善用compact** | 想保留摘要时使用 |
| **果断clear** | 上下文混乱时不要犹豫 |

### 斜杠命令优先级

**必知命令（生存必备）：**
- `/clear` - 清空上下文
- `/compact` - 压缩对话
- `/context` - 查看token使用
- `/help` - 显示帮助

**高频命令（日常使用）：**
- `/plan` - 规划任务
- `/init` - 初始化项目
- `/model` - 切换模型

**进阶命令（效率提升）：**
- 自定义命令
- `/mcp` - 管理MCP服务器
- `/hooks` - 配置钩子

---

## 实践练习（可选）

### 练习1：上下文管理（10分钟）

```bash
# 1. 开始一个复杂任务
请帮我重构整个认证系统

# 2. 观察上下文增长
/context

# 3. 使用/compact压缩
/compact

# 4. 继续任务
请继续重构

# 5. 最终清理
/clear
```

### 练习2：创建自定义命令（15分钟）

```bash
# 1. 创建一个自定义命令
cat > .claude/commands/optimize.md << 'EOF'
# 代码优化

优化 $ARGUMENTS 指定的代码：
1. 性能优化
2. 代码简化
3. 遵循最佳实践
EOF

# 2. 重启Claude Code
# 3. 使用命令
/optimize src/auth/userAuth.ts
```

### 练习3：完整工作流（30分钟）

```bash
# 从需求到部署的完整流程
# 1. 需求讨论
# 2. Plan模式规划
# 3. 增量开发
# 4. 测试
# 5. 审查
# 6. 部署
```
