# 🚀 ClaudeCode 培训文档 - 快速部署指南

## 📋 准备工作

1. ✅ 已安装 Git
2. ✅ 已安装 Node.js 18+
3. ✅ 已安装 VitePress
4. ✅ 有 GitHub 账号

## ⚡ 5分钟快速部署

### 第一步：创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称：`claudecode-docs`
3. 选择 **Public**（必须公开才能使用免费 Pages）
4. **不要**勾选任何初始化选项
5. 点击 "Create repository"

### 第二步：推送代码

在项目目录 `F:\02Doc\ClaudeCode` 执行：

```bash
# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/claudecode-docs.git

# 推送代码
git branch -M main
git push -u origin main
```

### 第三步：启用 GitHub Pages

1. 打开刚创建的仓库页面
2. 点击 **Settings** → **Pages**
3. 在 "Source" 下选择 **GitHub Actions**
4. 点击 **Save**

### 第四步：等待部署完成

1. 点击 **Actions** 查看部署进度
2. 等待约 1-2 分钟
3. 部署完成后访问：`https://YOUR_USERNAME.github.io/claudecode-docs/`

---

## 🎯 部署后需要做的事

### 1. 更新配置文件中的用户名

编辑 `.vitepress/config.mts`，将两处 `yourusername` 替换为你的 GitHub 用户名：

```typescript
// 找到这两行并修改
{ text: 'GitHub', link: 'https://github.com/YOUR_USERNAME/claudecode-docs' }
{ icon: 'github', link: 'https://github.com/YOUR_USERNAME/claudecode-docs' }
```

### 2. 提交并推送更改

```bash
git add .
git commit -m "chore: 更新GitHub用户名"
git push
```

### 3. 分享链接

部署完成后，你的同事可以通过以下链接访问文档：

```
https://YOUR_USERNAME.github.io/claudecode-docs/
```

---

## 📱 本地预览（可选）

在部署前，你可以在本地预览：

```bash
# 进入项目目录
cd F:\02Doc\ClaudeCode

# 启动开发服务器
npm run docs:dev

# 打开浏览器访问
http://localhost:5173
```

---

## 🔄 更新文档

当你需要更新文档时：

1. 修改 `docs/` 目录下的 `.md` 文件
2. 运行以下命令：
   ```bash
   git add .
   git commit -m "更新文档内容"
   git push
   ```
3. GitHub Actions 会自动重新部署

---

## 💡 提示

- 📌 仓库必须是 **Public** 才能使用免费的 GitHub Pages
- 📌 每次推送代码都会自动触发重新部署
- 📌 部署通常需要 1-2 分钟
- 📌 如遇问题，查看 GitHub Actions 的运行日志

---

## 🆘 需要帮助？

遇到问题？检查以下几点：

1. ✅ 确认仓库是 Public
2. ✅ 确认在 Settings → Pages 中选择了 GitHub Actions
3. ✅ 确认 `.vitepress/config.mts` 中的 `base` 是 `/claudecode-docs/`
4. ✅ 查看Actions标签，确认部署成功

仍有问题？请参考完整 README.md 文件。
